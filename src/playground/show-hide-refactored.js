let visibility = false;

const render = () =>{
    const template = (
        <div>
            <h1>Visibility Toggle</h1>
        <button>    {visibility ? 'Hide Details':'Show Details'} </button>
            </div>
    )
}